name: Backend Tests

on:
  push:
    branches:
      - main
      - develop
    paths:
      - projects/backend/**
      - .github/workflows/backend-tests.yml
      - .github/workflows/backend-test-suite.yml
  pull_request:
    branches:
      - main
      - develop
    paths:
      - projects/backend/**
      - .github/workflows/backend-tests.yml
      - .github/workflows/backend-test-suite.yml

permissions:
  contents: read

jobs:
  backend-tests:
    name: Backend test suite
    uses: ./.github/workflows/backend-test-suite.yml
