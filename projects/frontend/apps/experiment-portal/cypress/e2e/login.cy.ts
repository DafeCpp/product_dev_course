/// <reference types="cypress" />

describe('experiment-portal smoke', () => {
    it('opens login page', () => {
        cy.visit('/login')
        cy.contains('Вход в систему')
    })
})

/// <reference types="cypress" />

describe('experiment-portal smoke', () => {
    it('opens login page', () => {
        cy.visit('/login')
        cy.contains('Вход в систему')
    })
})

