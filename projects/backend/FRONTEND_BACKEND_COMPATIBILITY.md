# Анализ соответствия фронтенда и бэкенда

## 🔴 Критические несоответствия

### 1. Статусы Run
**Проблема:** Фронтенд использует статус `'completed'`, а бэкенд использует `'succeeded'`

**Фронтенд:**
```typescript
status: 'created' | 'running' | 'completed' | 'failed'
```

**Бэкенд:**
```python
RunStatus.DRAFT = "draft"
RunStatus.RUNNING = "running"
RunStatus.FAILED = "failed"
RunStatus.SUCCEEDED = "succeeded"  # ❌ Не совпадает с 'completed'
RunStatus.ARCHIVED = "archived"
```

**Где используется:**
- `runsApi.complete()` отправляет `{ status: 'completed' }` (строка 146)
- `runsApi.fail()` отправляет `{ status: 'failed', reason }` (строка 151)

**Решение:**
- ⚠️ **КРИТИЧНО:** Если фронтенд отправит `{ status: 'completed' }`, бэкенд вернет ошибку валидации, так как Pydantic ожидает `RunStatus` enum, который не содержит `'completed'`
- Вариант 1: Изменить фронтенд, чтобы использовать `'succeeded'` вместо `'completed'` (рекомендуется)
- Вариант 2: Добавить валидатор в `RunUpdateDTO` для преобразования `'completed'` → `'succeeded'`

---

### 2. Поле `params` vs `parameters` в Run
**Проблема:** Бэкенд использует `params`, фронтенд ожидает `parameters`

**Фронтенд:**
```typescript
interface Run {
  parameters: Record<string, any>  // ❌ Ожидает 'parameters'
}
```

**Бэкенд:**
```python
class Run(BaseModel):
    params: dict  # ❌ Использует 'params'
```

**Где используется:**
- `RunDetail.tsx:232` - отображает `run.parameters`
- `RunCreate` использует `parameters`

**Решение:**
- ⚠️ **КРИТИЧНО:** Фронтенд отправляет `parameters` в `RunCreate`, но бэкенд ожидает `params`. Это приведет к ошибке валидации.
- Вариант 1: Изменить фронтенд, чтобы использовать `params` (рекомендуется)
- Вариант 2: Добавить alias в `RunCreateDTO`: `parameters: dict = Field(alias="params")` и в `Run` модель для сериализации

---

### 3. Поле `finished_at` vs `completed_at` в Run
**Проблема:** Бэкенд использует `finished_at`, фронтенд ожидает `completed_at`

**Фронтенд:**
```typescript
interface Run {
  completed_at?: string  // ❌ Ожидает 'completed_at'
}
```

**Бэкенд:**
```python
class Run(BaseModel):
    finished_at: datetime | None = None  # ❌ Использует 'finished_at'
```

**Где используется:**
- `RunDetail.tsx:207-211` - отображает `run.completed_at`

**Решение:**
- ⚠️ **ПРОБЛЕМА:** Фронтенд читает `completed_at`, но бэкенд возвращает `finished_at`. Это приведет к `undefined` в UI.
- Вариант 1: Изменить фронтенд, чтобы использовать `finished_at` (рекомендуется)
- Вариант 2: Добавить `model_serializer` в `Run` модель для сериализации `finished_at` как `completed_at`

---

### 4. Поле `owner_id` vs `created_by` в Experiment
**Проблема:** Бэкенд использует `owner_id`, фронтенд ожидает `created_by`

**Фронтенд:**
```typescript
interface Experiment {
  created_by: string  // ❌ Ожидает 'created_by'
}
```

**Бэкенд:**
```python
class Experiment(BaseModel):
    owner_id: UUID  # ❌ Использует 'owner_id'
```

**Решение:**
- ⚠️ **ПРОБЛЕМА:** Фронтенд читает `created_by`, но бэкенд возвращает `owner_id`. Это приведет к `undefined` в UI.
- Вариант 1: Изменить фронтенд, чтобы использовать `owner_id` (рекомендуется)
- Вариант 2: Добавить `model_serializer` в `Experiment` модель для сериализации `owner_id` как `created_by`

---

### 5. Статусы Experiment
**Проблема:** Фронтенд использует `'created'`, а бэкенд использует `'draft'`

**Фронтенд:**
```typescript
status: 'created' | 'running' | 'completed' | 'failed' | 'archived'
```

**Бэкенд:**
```python
ExperimentStatus.DRAFT = "draft"  # ❌ Не совпадает с 'created'
ExperimentStatus.RUNNING = "running"
ExperimentStatus.FAILED = "failed"
ExperimentStatus.SUCCEEDED = "succeeded"  # ❌ Не совпадает с 'completed'
ExperimentStatus.ARCHIVED = "archived"
```

**Решение:**
- Вариант 1: Изменить фронтенд, чтобы использовать правильные статусы
- Вариант 2: Добавить маппинг в бэкенде

---

## 🟡 Потенциальные проблемы

### 6. Пагинация - поля `page` и `page_size`
**Статус:** ✅ **СОВПАДАЕТ**

**Фронтенд отправляет:**
- `page`, `page_size` в query параметрах

**Бэкенд принимает:**
- `page`, `page_size` через `pagination_params()` и преобразует в `limit`/`offset`

**Вывод:** Работает корректно

---

### 7. Структура ответов списков
**Статус:** ✅ **СОВПАДАЕТ**

**Фронтенд ожидает:**
```typescript
interface ExperimentsListResponse {
  experiments: Experiment[]
  total: number
  page: number
  page_size: number
}
```

**Бэкенд возвращает:**
```python
paginated_response(
    items,
    limit=limit,
    offset=offset,
    key="experiments",  # ✅ Совпадает
    total=total,
)
```

**Вывод:** Работает корректно

---

### 8. Capture Sessions - поле `get`
**Проблема:** Фронтенд вызывает `captureSessionsApi.get()`, но endpoint не реализован

**Фронтенд:**
```typescript
get: async (runId: string, sessionId: string): Promise<CaptureSession> => {
  const response = await apiClient.get(`/api/v1/runs/${runId}/capture-sessions/${sessionId}`)
  return response.data
}
```

**Бэкенд:**
- ❌ Endpoint `GET /api/v1/runs/{run_id}/capture-sessions/{session_id}` отсутствует
- Есть только `DELETE /api/v1/runs/{run_id}/capture-sessions/{session_id}`

**Решение:** Добавить endpoint `GET` для получения одной capture session

---

## ✅ Соответствует

### 9. Sensors API
- ✅ Все endpoints соответствуют
- ✅ Типы данных совпадают
- ✅ Параметры запросов совпадают

### 10. Telemetry API
- ✅ Endpoint соответствует
- ✅ Структура запроса совпадает
- ✅ Структура ответа совпадает

### 11. Experiments Search
- ✅ Endpoint реализован
- ✅ Параметры совпадают (`q`, `project_id`, `page`, `page_size`)

---

## 📋 Рекомендации по исправлению

### Приоритет 1 (Критично):
1. **Исправить статусы Run:**
   - Изменить фронтенд: `'completed'` → `'succeeded'`
   - Или добавить маппинг в бэкенде

2. **Исправить поле `params` → `parameters`:**
   - Добавить alias в модель Run: `parameters: dict = Field(alias="params")`
   - Или изменить фронтенд на `params`

3. **Исправить поле `finished_at` → `completed_at`:**
   - Добавить alias в модель Run: `completed_at: datetime | None = Field(alias="finished_at")`
   - Или изменить фронтенд на `finished_at`

4. **Исправить поле `owner_id` → `created_by`:**
   - Добавить alias в модель Experiment: `created_by: UUID = Field(alias="owner_id")`
   - Или изменить фронтенд на `owner_id`

### Приоритет 2 (Важно):
5. **Исправить статусы Experiment:**
   - Изменить фронтенд: `'created'` → `'draft'`, `'completed'` → `'succeeded'`

6. **Добавить endpoint GET для capture session:**
   - Реализовать `GET /api/v1/runs/{run_id}/capture-sessions/{session_id}`

---

## 🔧 Предпочтительный подход

**Рекомендуется изменить фронтенд**, так как:
1. Бэкенд следует техническому заданию и использует стандартные названия
2. OpenAPI спецификация уже определена с этими названиями
3. Изменение бэкенда может сломать другие клиенты

**Альтернатива:** Если нужно сохранить совместимость, можно добавить aliases в Pydantic модели для обратной совместимости.

---

## 📊 Итоговая сводка

### Критические проблемы (блокируют работу):
1. ❌ **Статус Run:** `'completed'` не существует в бэкенде → ошибка валидации
2. ❌ **Поле `parameters`:** фронтенд отправляет `parameters`, бэкенд ожидает `params` → ошибка валидации при создании Run
3. ❌ **Поле `completed_at`:** фронтенд читает `completed_at`, бэкенд возвращает `finished_at` → поле будет `undefined`
4. ❌ **Поле `created_by` в Experiment:** фронтенд читает `created_by`, бэкенд возвращает `owner_id` → поле будет `undefined`

### Важные проблемы (частично работают):
5. ⚠️ **Статусы Experiment:** `'created'` и `'completed'` не существуют → возможны проблемы с отображением
6. ⚠️ **Отсутствует endpoint:** `GET /api/v1/runs/{run_id}/capture-sessions/{session_id}` → метод `captureSessionsApi.get()` не работает

### Работает корректно:
- ✅ Sensors API
- ✅ Telemetry API
- ✅ Experiments Search
- ✅ Пагинация
- ✅ Структура ответов списков

---

## 🚨 Немедленные действия

**Перед запуском приложения необходимо исправить:**

1. **В `src/types/index.ts`:**
   - Изменить `Run.parameters` → `Run.params`
   - Изменить `Run.completed_at` → `Run.finished_at`
   - Изменить `Run.status` → добавить `'succeeded'` вместо `'completed'`, `'draft'` вместо `'created'`
   - Изменить `Experiment.created_by` → `Experiment.owner_id`
   - Изменить `Experiment.status` → добавить `'draft'` и `'succeeded'` вместо `'created'` и `'completed'`

2. **В `src/api/client.ts`:**
   - Изменить `runsApi.complete()` → отправлять `{ status: 'succeeded' }` вместо `'completed'`

3. **Во всех компонентах:**
   - Заменить `run.parameters` → `run.params`
   - Заменить `run.completed_at` → `run.finished_at`
   - Заменить `experiment.created_by` → `experiment.owner_id`
   - Обновить маппинг статусов в `StatusBadge` компонентах

