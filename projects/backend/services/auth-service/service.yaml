name: auth-service
description: Authentication Service
maintainers:
  - LostPointer

ports:
  - 8001:8001

database:
  # PostgreSQL connection pool settings
  # These settings are shared across all services for consistency
  pool_size: 20
  # Database URL is typically set via environment variable DATABASE_URL
  # but can be overridden here if needed
  # url: postgresql://postgres:postgres@localhost:5432/auth_db

